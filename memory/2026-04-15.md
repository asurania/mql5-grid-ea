- Built end-to-end v1 Forex event-risk pipeline from research to runtime integration.
- Event-risk ML stack now includes leak-safe training dataset, pre-event price regime features, XGBoost baseline, improved price-context model, dedicated `y_avoid_session` model, and calibrated live thresholds.
- Recommended live avoid-session thresholds: score >= 0.75 => avoid_session_strong, >= 0.60 => avoid_session, >= 0.45 => review_or_halt_new_entries.
- Added production-style Python master runtime components: `run_live_event_risk_inference.py`, `build_pair_risk_policy.py`, `publish_pair_risk_policy.py`, and `run_event_risk_master.py`.
- Documented integration contracts in `docs/forex_event_risk_inference_spec.md`, `docs/forex_event_risk_master_runtime.md`, `docs/mt5_file_handoff.md`, `docs/mql5_pair_risk_policy_contract.md`, and `docs/mql5_slave_architecture.md`.
- Established MT5 handoff convention: Python publishes pair policy to `runtime_handoff/mt5_common/Files/ForexSlave/pair_risk_policy.json`; MQL5 expects `ForexSlave\\pair_risk_policy.json` relative to MT5 common files.
- Built fresh MQL5 slave skeleton under `MQL5/` with EA shell plus `Types`, `Config`, `TelemetryLogger`, `PolicyReader`, `ExecutionGate`, and `TradeExecutor` modules.
- MQL5 side now has schema-specific file-backed policy reading, fail-closed execution-gate behavior for missing/invalid/stale policy, and a real `CTrade`-based trade executor wrapper foundation.
- User asked whether to convert old MQL4 EA first; recommendation was to build new MQL5 slave mostly from scratch, using MQL4 only as behavioral reference, not as a blind conversion source.
- Current best next engineering step is to build `PositionRegistry` for MQL5, then later attach grid/basket management logic on top of the cleaned execution and policy boundary.

## Session 2 progress (afternoon)

- Built price-only regime dataset: `build_regime_dataset.py` → `data/processed/regime/fx_regime_dataset.parquet` (959K rows, 4 pairs, 2023-2026)
- Regime labels: 40% neutral, 41% range, 16% trend (using trend_frac thresholds)
- 3-class regime classifier (XGBoost): 0.43 accuracy, trend class near zero recall — too noisy
- Binary trending classifier: test ROC AUC 0.605, PR AUC 0.200 — barely above random
- **Conclusion**: Minute-level price features alone don't separate trending from ranging well enough. Regime classifier needs higher-timeframe resampling + richer indicators (ADX, ATR ratios, Hurst). De-prioritized for v1.

- Built `y_trend_danger` training dataset and model from existing event-risk features
- Initial run had target leakage via `y_trend_danger_right` column (join duplicate)
- After fixing leakage: test ROC AUC 0.562, PR AUC 0.145 — pre-event features alone can't predict trend danger well

- Trained LightGBM avoid-session model: test ROC AUC 0.719, PR AUC 0.427 (vs XGBoost 0.706 / 0.418)
- Built ensemble (XGBoost + LightGBM probability average):
  - Test ROC AUC: 0.715
  - Test PR AUC: 0.426
  - **Strong-avoid precision at 0.75: 93.3%** (up from 91.9% XGBoost-only)
  - **Standard-avoid precision at 0.60: 76.0%** (up from 69.7% XGBoost-only)

- Swapped live inference pipeline (`run_live_event_risk_inference.py`) to use ensemble model
  - Now loads both XGBoost and LightGBM, averages probabilities
  - Output includes `score_xgb`, `score_lgbm`, and `score_avoid_session` (ensemble)
  - Model field: `ensemble_xgb_lgbm`, threshold version: `avoid_session_ensemble_v1`
- Updated inference spec doc to reflect ensemble model

### Model comparison summary
| Model | Target | Test ROC AUC | Test PR AUC | Strong-avoid precision @0.75 |
|---|---|---|---|---|
| XGBoost | y_avoid_session | 0.706 | 0.418 | 91.9% |
| LightGBM | y_avoid_session | 0.719 | 0.427 | — |
| **Ensemble** | y_avoid_session | **0.715** | **0.426** | **93.3%** |
| XGBoost | y_trend_danger | 0.562 | 0.145 | N/A |
| Binary XGBoost | is_trending | 0.605 | 0.200 | N/A |

## Session 3 progress (late afternoon)

- Built v2 entry intent signal (`build_entry_intent_v2.py`)
  - Replaces simple "all timeframes same sign" rule with weighted alignment scoring
  - Added volatility regime filter: rejects dead chop (ATR frac < 0.0003) and extreme vol (ATR frac > 0.008)
  - Added trend strength filter: requires 0.0002–0.015 range
  - Added short-term counter-move detection: skips if 5m opposes direction strongly
  - Debug field includes alignment score, vol regime, trend strength for diagnostics
  - Updated master runtime to use v2 entry intent
  - Updated contract doc with v2 schema and logic description
- Trend danger v2 model (with ATR/momentum/surprise features): test ROC AUC 0.513, worse than v1 (0.562)
  - Conclusion: adding more features made it worse, confirming that trend danger is fundamentally unpredictable pre-event

- Continued MT5 dry-run diagnosis for ForexSlaveEA on demo account. Refreshed Python runtime outputs and copied fresh JSON handoff files. Discovered running MT5 instance is browsing `C:\Program Files\MetaTrader 5\MQL5\Files\ForexSlave` rather than the earlier assumed terminal common-files path. Despite copying fresh `pair_risk_policy.json`, `entry_intent.json`, and `grid_policy.json` into both candidate locations, EA logs still show `status=stale_fail_safe`, `BLOCK_NEW_TRADE :: stale policy exceeded fail-safe threshold`, and `GRID_SKIP_NEW_ENTRY`. Strong suspicion now is MQL5 path/config or freshness parsing mismatch rather than simple file copy failure. Next step is to inspect `InpPolicyFilePath`, `PolicyReader`, and freshness logic in the EA code.
