- Investigated live MT5 EA issues reported by user on 2026-04-22 morning run.
- Confirmed `data/live/policy/entry_intent.json` is currently hardcoded sell-only for NZDUSD, GBPJPY, GBPUSD, which explains why first entries never seed long baskets.
- Confirmed `data/live/policy/core_portfolio_policy.json` exports `grid_mode: both_sides` but `seed_mode: single_side`, so live EA only seeds one side and depends on grid expansion to ever create the opposite side.
- Identified likely live grid-expansion blocker in `mt5/Include/ForexSlave/GridManager.mqh`: expansion side is chosen with `useBuy = (buyTrades <= sellTrades)`, but when one side has zero positions, the chosen side can have `lastOpenPrice == 0`, causing `invalid_price_state` and preventing the first opposite-side leg from ever being opened.
- Confirmed live lot sizing in `core_portfolio_policy.json` is exported as fixed `initial_lot: 0.01` for all three pairs even though `initial_lot_mode` is `equity_scaled`; current MQL5 `CorePortfolioPolicyReader::GetInitialLot()` just returns the exported value and does not re-scale by actual account equity.
- Working diagnosis for the three user-reported issues: (1) no longs because entry intent is sell-only and opposite-side first leg is blocked, (2) no grid expansion because opposite-side bootstrap leg cannot initialize from zero-position state, (3) lot sizing is not dynamically scaled on MT5 side despite equity_scaled metadata.

- Investigated suspicious NZDUSD Asia event gating count (only 1 skipped session) and found the root cause in `src/calendar_pipeline/build_session_avoidance.py`: it was assigning event sessions using crude UTC hour bands instead of Calgary-time session definitions.
- Patched session labeling in `build_session_avoidance.py` to use Calgary-time windows aligned with trading sessions (Asia 16:00–21:00, London 21:00–04:00, New York 04:00–12:00 local), then rebuilt `events_with_session_avoidance.parquet`.
- After fix, historical avoidance counts shifted materially: GBPJPY Asia/London 309/509, GBPUSD Asia/London 2/509, NZDUSD Asia/London 21/0; rerun of Asia event-gated backtest showed total gated sessions 192 with pair counts GBPJPY 176, GBPUSD 2, NZDUSD 14.
- User requested current settings for NZDUSD and GBPJPY; shared current live-policy values from active policy stack.
- User requested exploratory Asia-only GBPJPY backtests with temporary overrides. Results communicated:
  - Max basket drawdown $250 only: total pnl about -6551.20 (worse than prior event-gated Asia GBPJPY result).
  - Grid levels 6 and step pips 8: total pnl about -5936.38 (improvement but still negative).
  - Grid levels 6, step pips 8, multiplier 1.1: total pnl about -5764.81, worst drawdown about 107.33, event-gated sessions 176 (slight improvement, still clearly negative).
- Key current research takeaway: GBPJPY in Asia remains structurally weak even after event-gating fix and parameter tweaks; issue is not primarily news gating or basket DD cap tightness.
# 2026-04-22

- User is actively tuning OpenClaw model routing and fallback order.
- Confirmed local `ollama/qwen3-coder-next` is available and can run fully on GPU.
- Machine appears to expose about 62 GB of GPU-addressable memory to Ollama/ROCm; `ollama ps` showed a 61 GB model running at 100% GPU.
- Model stack changed multiple times today; most recent durable preference before flush was:
  - Primary: `ollama/qwen3-coder-next`
  - Fallback 1: `openai-codex/gpt-5.4`
  - Fallback 2: `ollama/kimi-k2.6:cloud`
- User is interested in Alberta daylight-saving/time-change news and asked about Premier Smith’s comments this week.
- Investigated live MT5 EA issues reported by user on 2026-04-22 morning run.
- Confirmed `data/live/policy/entry_intent.json` is currently hardcoded sell-only for NZDUSD, GBPJPY, GBPUSD, which explains why first entries never seed long baskets.
- Confirmed `data/live/policy/core_portfolio_policy.json` exports `grid_mode: both_sides` but `seed_mode: single_side`, so live EA only seeds one side and depends on grid expansion to ever create the opposite side.
- Identified likely live grid-expansion blocker in `mt5/Include/ForexSlave/GridManager.mqh`: expansion side is chosen with `useBuy = (buyTrades <= sellTrades)`, but when one side has zero positions, the chosen side can have `lastOpenPrice == 0`, causing `invalid_price_state` and preventing the first opposite-side leg from ever being opened.
- Confirmed live lot sizing in `core_portfolio_policy.json` is exported as fixed `initial_lot: 0.01` for all three pairs even though `initial_lot_mode` is `equity_scaled`; current MQL5 `CorePortfolioPolicyReader::GetInitialLot()` just returns the exported value and does not re-scale by actual account equity.
- Working diagnosis for the three user-reported issues: (1) no longs because entry intent is sell-only and opposite-side first leg is blocked, (2) no grid expansion because opposite-side bootstrap leg cannot initialize from zero-position state, (3) lot sizing is not dynamically scaled on MT5 side despite equity_scaled metadata.

- Investigated suspicious NZDUSD Asia event gating count (only 1 skipped session) and found the root cause in `src/calendar_pipeline/build_session_avoidance.py`: it was assigning event sessions using crude UTC hour bands instead of Calgary-time session definitions.
- Patched session labeling in `build_session_avoidance.py` to use Calgary-time windows aligned with trading sessions (Asia 16:00–21:00, London 21:00–04:00, New York 04:00–12:00 local), then rebuilt `events_with_session_avoidance.parquet`.
- After fix, historical avoidance counts shifted materially: GBPJPY Asia/London 309/509, GBPUSD Asia/London 2/509, NZDUSD Asia/London 21/0; rerun of Asia event-gated backtest showed total gated sessions 192 with pair counts GBPJPY 176, GBPUSD 2, NZDUSD 14.
- User requested current settings for NZDUSD and GBPJPY; shared current live-policy values from active policy stack.
- User requested exploratory Asia-only GBPJPY backtests with temporary overrides. Results communicated:
  - Max basket drawdown $250 only: total pnl about -6551.20 (worse than prior event-gated Asia GBPJPY result).
  - Grid levels 6 and step pips 8: total pnl about -5936.38 (improvement but still negative).
  - Grid levels 6, step pips 8, multiplier 1.1: total pnl about -5764.81, worst drawdown about 107.33, event-gated sessions 176 (slight improvement, still clearly negative).
- Key current research takeaway: GBPJPY in Asia remains structurally weak even after event-gating fix and parameter tweaks; issue is not primarily news gating or basket DD cap tightness.
# 2026-04-22

- User is actively tuning OpenClaw model routing and fallback order.
- Confirmed local `ollama/qwen3-coder-next` is available and can run fully on GPU.
- Machine appears to expose about 62 GB of GPU-addressable memory to Ollama/ROCm; `ollama ps` showed a 61 GB model running at 100% GPU.
- Model stack changed multiple times today; most recent durable preference before flush was:
  - Primary: `ollama/qwen3-coder-next`
  - Fallback 1: `openai-codex/gpt-5.4`
  - Fallback 2: `ollama/kimi-k2.6:cloud`
- User is interested in Alberta daylight-saving/time-change news and asked about Premier Smith’s comments this week.

- Troubleshooting context overflow/compaction timeout error on qwen3-coder-next:
  - model advertises 262k context but Ollama loaded at 128k
  - compaction timing out (~5 min) caused overflow errors during long chats
  - aligned OpenClaw contextWindow config with Ollama's actual loaded context (128k)
  - fix applied in gateway restart config-patch
  - recommended systemd override to raise OLLAMA_CONTEXT_LENGTH to 262144 for full headroom
